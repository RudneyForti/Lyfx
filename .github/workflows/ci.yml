name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  quality:
    name: Typecheck, lint and build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
        env:
          # Build-only placeholders — no real service is reached during next build
          DATABASE_URL: postgresql://postgres:ci@localhost:5432/ci
          SESSION_SECRET: ci-placeholder-secret
          ADMIN_SECRET: ci-placeholder-admin
          NEXT_PUBLIC_GOOGLE_MAPS_API_KEY: ci-placeholder

  # Test job is added in Phase 1 (Vitest + Playwright + MSW).
  # It becomes a required status check once the suite exists.
